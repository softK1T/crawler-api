fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx[http2]>=0.25.2
celery>=5.3.4
redis>=5.0.1
gevent>=24.2.1
starlette>=0.27.0
# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
# HTML parsing & extraction
beautifulsoup4>=4.12.0
lxml>=5.0.0
html2text>=2024.2.26
# Stealth crawling — TLS impersonation (bypasses Cloudflare JA3 fingerprinting)
curl-cffi>=0.7.0
# Browser crawling — pinned to match camoufox binary protocol bundle
playwright==1.49.0
# Anti-detect browser (bypasses Cloudflare high, device fingerprinting, Shopee)
# Install: pip install camoufox[geoip] && python -m camoufox fetch
camoufox[geoip]>=0.4.0
# Webshare API sync
httpx>=0.27.0
# API key hashing (argon2id)
argon2-cffi>=23.1.0
# Domain extraction for policy resolution
tldextract>=5.1.0
# WARC archival
warcio>=1.7.4
# Async S3 client (AWS / MinIO)
aioboto3>=13.0
# Async job queue (replaces Celery for new code paths)
arq>=0.25.0
